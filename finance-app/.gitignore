# Streamlit secrets (contains sensitive API keys and credentials)
.streamlit/secrets.toml

# Test fixtures (large binary statement samples — kept local-only)
tests/raw-test-data/
*.log

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
env/
venv/
*.local

# Local-only state (NOT for git)
mock_db.json
.env
state/
logs/

# Scrape inbox/state (statement files dropped here for ingest; gitignored)
scrape/.env
scrape/inbox/
scrape/state/
scrape/logs/
